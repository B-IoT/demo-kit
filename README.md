# Demo kit

## Installation

First of all, download and install Node.js and npm from [here](https://nodejs.org/en/).

Then, open the terminal and install `serve` with:

```shell
npm install -g serve
```

## Usage

Run the frontend with:

```shell
serve -s build
```

The website will be served [here](http://localhost:5000).
